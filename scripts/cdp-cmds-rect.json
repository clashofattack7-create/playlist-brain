[{"method":"Runtime.evaluate","params":{"expression":"(() => { const b = Array.from(document.querySelectorAll('button')).find(x => /login/i.test((x.innerText||'').replace(/\\s+/g,' ').trim()) && x.offsetParent!==null); if (!b) return 'none'; const r = b.getBoundingClientRect(); return JSON.stringify({x: r.x + r.width/2, y: r.y + r.height/2, w: r.width, h: r.height}); })()","returnByValue":true}}]